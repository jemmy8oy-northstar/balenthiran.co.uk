# Antigravity AI Instructions

- **Do NOT use the browser tool**. I prefer to verify changes manually or have you explain them in detail.
- **Strictly No Browser**: Under no circumstances should the `browser_subagent` or `read_browser_page` tools be used unless I explicitly ask you to in a specific turn.
- **Focus on Code and Logic**: Prioritize deep code analysis, architectural planning, and precise implementation over visual verification. 
- **Proactive Sprint Planning**: Always check if the next sprint is planned in `frontend/src/data/sprints.json`. If missing, proactively suggest creating it and copy forward the current board state.
